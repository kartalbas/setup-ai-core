Documents written for agents, project-wide: what an agent must know and the code cannot say, the
architecture decisions among them; never a second copy of what the code already says.
`ai-core init` copies this folder to `.ai-core/docs/<this harness>/` of every checkout, and the map
of each repository points at what matters there.
