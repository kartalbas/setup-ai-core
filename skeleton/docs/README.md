Documents written for agents, project-wide: a glossary, the naming conventions, the architecture.
`ai-core init` copies this folder to `.ai-core/docs/<this harness>/` of every checkout, and the map
of each repository points at what matters there.
