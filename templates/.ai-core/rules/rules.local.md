# Project-Specific Rules

setup-ai-core updates never overwrite this file, and git does not track it (see .git/info/exclude). Add this project's conventions, overrides
and instructions here. Where a rule here conflicts with `rules.md`, this file wins.
